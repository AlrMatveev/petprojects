import { Box } from "@mui/material";

function Header() {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        m: 5,
        textAlign: "center",
      }}
    >
      JS + CSS
    </Box>
  );
}

export default Header;
